# Tableau Package

Run this command from the project root to refresh the Tableau data package:

```bash
python3 tableau/build_tableau_package.py
```

The generated Tableau-ready files are written to:

```text
tableau/tableau_data/
```

Open `Tableau_Dashboard_Build_Guide.md` for worksheet and dashboard build
instructions.

